apiVersion: v1
kind: Pod
metadata:
  name: wuvt-site-redis-prod
  labels:
    app: wuvt-site-redis
    instance: prod
spec:
  containers:
  - name: redis
    image: redis:3.2-alpine
    args:
    - '/etc/redis/redis.conf'
    ports:
    - containerPort: 6379
    volumeMounts:
    - name: redis-config
      mountPath: /etc/redis
      readOnly: true
    - name: redis-data
      mountPath: /data
  volumes:
  - name: redis-config
    secret:
      secretName: wuvt-site-redis-prod
  - name: redis-data
    hostPath:
      path: /media/local-storage/wuvt-site-redis-prod-data
