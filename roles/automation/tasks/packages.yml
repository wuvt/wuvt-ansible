---
- name: Apt update
  apt:
    update_cache: yes
    upgrade: yes
  tags:
  - apt
  - packages

- name: Install automation packages
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
  - python3
  - python3-requests
  - python3-yaml
  tags:
  - packages
